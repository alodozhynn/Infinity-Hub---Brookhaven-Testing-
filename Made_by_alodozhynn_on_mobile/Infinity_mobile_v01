local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/shlexware/Orion/main/source"))()
local Window = OrionLib:MakeWindow({Name = "Infinity Mobile Brookhaven 🏡", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest"})

local mainTab = Window:MakeTab({
    Name = "Main/Trolling",
    Icon = "rbxassetid://18837606113",
    PremiumOnly = false
})

local visualTab = Window:MakeTab({
    Name = "Teleportes + Visual/Cliente",
    Icon = "rbxassetid://18837725784",
    PremiumOnly = false
})

local adminTab = Window:MakeTab({
    Name = "Admin Scripts",
    Icon = "rbxassetid://18837697300",
    PremiumOnly = false
})

local itemsTab = Window:MakeTab({
    Name = "Alguns itens",
    Icon = "rbxassetid://18837606113",
    PremiumOnly = false
})

OrionLib:MakeNotification({
    Name = "Infinityyy was loaded !",
    Content = "This script was made by Alodozhynn",
    Image = "rbxassetid://4483345998",
    Time = 5
})

-- Função de voo
local uis = game:GetService("UserInputService")
local rs = game:GetService("RunService")

local myPlayer = game.Players.LocalPlayer
local myChar = myPlayer.Character
local myHRP = myChar:WaitForChild("HumanoidRootPart")
local camera = game.Workspace.CurrentCamera

local flying = false
local speed = 0.5

local bp = Instance.new("BodyPosition", myHRP)
bp.MaxForce = Vector3.new()
bp.D = 10
bp.P = 10000

local bg = Instance.new("BodyGyro", myHRP)
bg.MaxTorque = Vector3.new()
bg.D = 10

function fly()
    flying = true
    bp.MaxForce = Vector3.new(400000, 400000, 400000)
    bg.MaxTorque = Vector3.new(400000, 400000, 400000)
    while flying do
        rs.RenderStepped:wait()
        bp.Position = myHRP.Position + ((myHRP.Position - camera.CFrame.p).unit * speed)
        bg.CFrame = CFrame.new(camera.CFrame.p, myHRP.Position)
    end
end

function endFlying()
    bp.MaxForce = Vector3.new()
    bg.MaxTorque = Vector3.new()
    flying = false
end

uis.InputBegan:connect(function(input)
    if input.KeyCode == Enum.KeyCode.Space then
        if not flying then
            fly()
        else
            endFlying()
        end
    end
end)

-- Função para teleportar jogador e matar jogador
local function teleportAndKillPlayer(targetPlayer)
    local targetHRP = targetPlayer.Character:WaitForChild("HumanoidRootPart")
    myHRP.CFrame = CFrame.new(-83, 19, -130)
    wait(1)  -- Esperar um segundo para garantir que o teleporte inicial foi concluído
    local connection
    connection = game:GetService("RunService").Stepped:Connect(function()
        myHRP.CFrame = targetHRP.CFrame
        -- Verifique se o jogador alvo está sentado
        if targetPlayer.Character and targetPlayer.Character:FindFirstChild("Humanoid") and targetPlayer.Character.Humanoid.Sit then
            -- Jogador alvo está sentado, pare de teletransportar
            myHRP.CFrame = CFrame.new(500000, 0, 0)
            connection:Disconnect()
        end
    end)
end

-- Lista de jogadores para selecionar
local players = game.Players:GetPlayers()
local selectedPlayer

local dropdownOptions = {}
for i, player in ipairs(players) do
    table.insert(dropdownOptions, player.Name)
end

-- Adiciona a nova seção "Trollar jogadores"
mainTab:AddSection({
    Name = "Trollar jogadores"
})

mainTab:AddDropdown({
    Name = "Selecionar Jogador",
    Default = "",
    Options = dropdownOptions,
    Callback = function(value)
        selectedPlayer = game.Players:FindFirstChild(value)
    end
})

mainTab:AddButton({
    Name = "Matar Jogador [precisa de um equipamento]",
    Callback = function()
        if selectedPlayer then
            teleportAndKillPlayer(selectedPlayer)
        else
            print("Nenhum jogador selecionado.")
        end
    end    
})

mainTab:AddButton({
    Name = "Enviar pro Infinito",
    Callback = function()
        myHRP.CFrame = CFrame.new(0, 1000000, 0)
    end    
})

-- Atualiza a aba "Teleportes + Visual/Cliente"
visualTab:AddSection({
    Name = "Teleporte para outros lugares (brookhaven only)"
})

visualTab:AddButton({
    Name = "Delegacia",
    Callback = function()
        myHRP.CFrame = CFrame.new(-107, 3, 35)
    end    
})

visualTab:AddButton({
    Name = "Hospital",
    Callback = function()
        myHRP.CFrame = CFrame.new(-333, 4, 36)
    end    
})

visualTab:AddButton({
    Name = "Escola",
    Callback = function()
        myHRP.CFrame = CFrame.new(-324, 4, 203)
    end    
})

visualTab:AddButton({
    Name = "Banco do Brookhaven",
    Callback = function()
        myHRP.CFrame = CFrame.new(12, 3, 268)
    end    
})

visualTab:AddButton({
    Name = "Mercado",
    Callback = function()
        myHRP.CFrame = CFrame.new(-23, 3, -128)
    end    
})

visualTab:AddButton({
    Name = "Shopping",
    Callback = function()
        myHRP.CFrame = CFrame.new(161, 11, -166)
    end    
})

visualTab:AddButton({
    Name = "Aeroporto",
    Callback = function()
        myHRP.CFrame = CFrame.new(356, 14, 107)
    end    
})

visualTab:AddButton({
    Name = "Início/Lobby",
    Callback = function()
        myHRP.CFrame = CFrame.new(-10, 23, 53)
    end    
})

-- Adiciona a aba "Alguns itens" com a seção "Itens de lugares"
itemsTab:AddSection({
    Name = "Itens de lugares"
})

itemsTab:AddButton({
    Name = "Bola de Basquete",
    Callback = function()
        myHRP.CFrame = CFrame.new(-137, 2, 98)
    end    
})

itemsTab:AddButton({
    Name = "Macas de Hospital",
    Callback = function()
        myHRP.CFrame = CFrame.new(-343, 2, 34)
    end    
})

itemsTab:AddButton({
    Name = "Aparelho Desconhecido",
    Callback = function()
        myHRP.CFrame = CFrame.new(-272, 2, 60)
    end    
})

itemsTab:AddButton({
    Name = "Aparelho de Zoom",
    Callback = function()
        myHRP.CFrame = CFrame.new(-316, 5, 62)
    end    
})

itemsTab:AddButton({
    Name = "Medicamento",
    Callback = function()
        myHRP.CFrame = CFrame.new(-336, 3, 28)
    end    
})

itemsTab:AddButton({
    Name = "Livro",
    Callback = function()
        myHRP.CFrame = CFrame.new(-333, 4, 170)
    end    
})

itemsTab:AddButton({
    Name = "Guitarra/Violão",
    Callback = function()
        myHRP.CFrame = CFrame.new(-384, 17, 212)
    end    
})

itemsTab:AddButton({
    Name = "Cartão do Cofre",
    Callback = function()
        myHRP.CFrame = CFrame.new(6, 2, 269)
    end    
})

itemsTab:AddButton({
    Name = "Muleta de Dinheiro",
    Callback = function()
        myHRP.CFrame = CFrame.new(-11, 16, 275)
    end    
})

itemsTab:AddButton({
    Name = "Armas/Bombas",
    Callback = function()
        myHRP.CFrame = CFrame.new(-138, -29, 239)
    end    
})

itemsTab:AddButton({
    Name = "Sofá do Club Brooks",
    Callback = function()
        myHRP.CFrame = CFrame.new(-83, 19, -130)
    end    
})

-- Botões para a aba Admin Scripts
adminTab:AddButton({
    Name = "Infinite Yield",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
    end    
})

adminTab:AddButton({
    Name = "Nameless Admin",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/FilteringEnabled/NamelessAdmin/main/Source'))()
    end    
})
-- Cria uma nova tab chamada "Chat"
local chatTab = Window:MakeTab({
    Name = "Chat",
    Icon = "rbxassetid://18837606113",
    PremiumOnly = false
})

-- Adiciona um label com a dica
chatTab:AddLabel("Dica: Você quer mandar uma mensagem falsa? 👇")

-- Adiciona uma seção chamada "Mensagens falsas (still beta, may have errors)"
chatTab:AddSection({
    Name = "Mensagens falsas (still beta, may have errors)"
})

-- Adiciona um botão para enviar a mensagem de shutdown
chatTab:AddButton({
    Name = "Mensagem de shutdown",
    Callback = function()
        local player = game.Players.LocalPlayer
        local message = "texto do n seeei\n\n[Mensagem do servidor] O servidor vai ser derrubado em 118 segundos, por problemas de conexão com nossos servidores da plataforma."
        game.ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(message, "All")
    end    
})

-- Adiciona um botão para enviar a mensagem global falsa
chatTab:AddButton({
    Name = "Mensagem global, mas falsa",
    Callback = function()
        local player = game.Players.LocalPlayer
        local message = "normal né, dboa\n\n[Mensagem global do desenvolvedor] hello guys how are u all im wolf paq loool asdf asdf abcdefgh trrtyggttt"
        game.ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(message, "All")
    end    
})
-- Aba "Veículos"
local veiculosTab = Window:MakeTab({
    Name = "Veículos",
    Icon = "rbxassetid://18837725784",
    PremiumOnly = false
})

-- Adiciona a seção "Trollagem usando carros" na aba "Veículos"
veiculosTab:AddSection({
    Name = "Trollagem usando carros"
})

veiculosTab:AddLabel({
    Name = "Dica: Você já experimentou voar com um carro? Tente agora, no botão abaixo!"
})

veiculosTab:AddButton({
    Name = "Voar com um carro",
    Callback = function()
        loadstring(game:HttpGet("https://pastebin.com/raw/GRp9rP98"))()
    end    
})

veiculosTab:AddButton({
    Name = "Deletar carro",
    Callback = function()
        local player = game.Players.LocalPlayer
        local char = player.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")

        if hrp and hrp.Parent and hrp.Parent:IsA("VehicleSeat") then
            -- Resetar o jogador se estiver sentado em um carro
            player.Character:BreakJoints()
        else
            OrionLib:MakeNotification({
                Name = "Você precisa entrar no carro",
                Content = "Você só pode remover um carro se entrar nele.",
                Image = "rbxassetid://4483345998",
                Time = 5
            })
        end
    end    
})

-- Aba "Chat"
local chatTab = Window:MakeTab({
    Name = "Chat",
    Icon = "rbxassetid://18837606113",
    PremiumOnly = false
})

-- Adiciona a seção "Mensagens falsas (still beta, may have errors)" na aba "Chat"
chatTab:AddSection({
    Name = "Mensagens falsas (still beta, may have errors)"
})

chatTab:AddLabel({
    Name = "Dica: Você quer mandar uma mensagem falsa? 👇"
})

chatTab:AddButton({
    Name = "Mensagem de shutdown",
    Callback = function()
        game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(
            "texto do n seeei\n[Mensagem do servidor] O servidor vai ser derrubado em 118 segundos, por problemas de conexão com nossos servidores da plataforma.",
            "All"
        )
    end    
})

chatTab:AddButton({
    Name = "Mensagem global, mas falsa",
    Callback = function()
        game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(
            "normal né, dboa\n[Mensagem global do desenvolvedor] hello guys how are u all im wolf paq loool asdf asdf abcdefgh trrtyggttt",
            "All"
        )
    end    
})
-- Aba "Tutoriais"
local tutorialsTab = Window:MakeTab({
    Name = "Tutoriais",
    Icon = imageID,
    PremiumOnly = false
})

-- Adiciona a seção e os labels na aba "Tutoriais"
tutorialsTab:AddSection({
    Name = "Tutoriais"
})

tutorialsTab:AddLabel({
    Name = "Como ser desbanido de uma casa? saia do jogo e entre dnv."
})

tutorialsTab:AddLabel({
    Name = "Como spawnar um carro premium sem ter premium? sla, não tenho acesso aos armazenamentos do jogo."
})

tutorialsTab:AddLabel({
    Name = "Como parar de sofrer bullying por ter cabeça pequena? só usar esse id '1048037'."
})

tutorialsTab:AddLabel({
    Name = "Como ter um amigo que mora em um abacaxi no fundo do mar? só usar esse id: 208660809."
})
-- Cria a nova tab "Casas"
local casasTab = Window:MakeTab({
    Name = "Casas",
    Icon = "rbxassetid://8834748103",
    PremiumOnly = false
})

-- Adiciona a seção "Ser desbanido de uma casa"
casasTab:AddSection({
    Name = "Ser desbanido de uma casa"
})

-- Adiciona o botão "Método 1, entrar dnv"
casasTab:AddButton({
    Name = "Método 1, entrar dnv",
    Callback = function()
        -- Reentra no jogo no mesmo servidor
        local player = game.Players.LocalPlayer
        if player then
            -- Teleporta para o mesmo servidor
            local teleportService = game:GetService("TeleportService")
            teleportService:Teleport(game.PlaceId, player)
        end
    end
})

-- Adiciona o botão "Método 2: não vai funcionar no Delta, tenho certeza dhshsjsbahsb"
casasTab:AddButton({
    Name = "Método 2: não vai funcionar no Delta, tenho certeza dhshsjsbahsb",
    Callback = function()
        -- Notificação indicando que o método não funcionará
        OrionLib:MakeNotification({
            Name = "Método 2",
            Content = "Agora não amigo, esse script é pro Delta Executor e não vai funcionar.",
            Image = "rbxassetid://8834748103",
            Time = 5
        })
    end
})
-- Inicializando a biblioteca Orion
OrionLib:Init()
