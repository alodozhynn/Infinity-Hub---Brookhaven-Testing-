local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/shlexware/Orion/main/source"))()
local Window = OrionLib:MakeWindow({Name = "Infinity Nightscript Brookhaven 🏡", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest" }) 

local mainTab = Window:MakeTab({
    Name = "Main/Trolling",
    Icon = "rbxassetid://8834748103",
    PremiumOnly = false
})

local visualTab = Window:MakeTab({
    Name = "Teleportes + Visual/Cliente",
    Icon = "rbxassetid://8834748103",
    PremiumOnly = false
})

local adminTab = Window:MakeTab({
    Name = "Admin Scripts",
    Icon = "rbxassetid://8834748103",
    PremiumOnly = false
})

local itemsTab = Window:MakeTab({
    Name = "Alguns itens",
    Icon = "rbxassetid://8834748103",
    PremiumOnly = false
})

OrionLib:MakeNotification({
    Name = "Infinityyy was loaded !",
    Content = "This script was made by Alodozhynn",
    Image = "rbxassetid://4483345998",
    Time = 5
})

-- Função de voo
local uis = game:GetService("UserInputService")
local rs = game:GetService("RunService")

local myPlayer = game.Players.LocalPlayer
local myChar = myPlayer.Character
local myHRP = myChar:WaitForChild("HumanoidRootPart")
local camera = game.Workspace.CurrentCamera

local flying = false
local speed = 0.5

local bp = Instance.new("BodyPosition", myHRP)
bp.MaxForce = Vector3.new()
bp.D = 10
bp.P = 10000

local bg = Instance.new("BodyGyro", myHRP)
bg.MaxTorque = Vector3.new()
bg.D = 10

function fly()
    flying = true
    bp.MaxForce = Vector3.new(400000, 400000, 400000)
    bg.MaxTorque = Vector3.new(400000, 400000, 400000)
    while flying do
        rs.RenderStepped:wait()
        bp.Position = myHRP.Position + ((myHRP.Position - camera.CFrame.p).unit * speed)
        bg.CFrame = CFrame.new(camera.CFrame.p, myHRP.Position)
    end
end

function endFlying()
    bp.MaxForce = Vector3.new()
    bg.MaxTorque = Vector3.new()
    flying = false
end

uis.InputBegan:connect(function(input)
    if input.KeyCode == Enum.KeyCode.Space then
        if not flying then
            fly()
        else
            endFlying()
        end
    end
end)

-- Função para teleportar jogador e matar jogador
local function teleportAndKillPlayer(targetPlayer)
    local targetHRP = targetPlayer.Character:WaitForChild("HumanoidRootPart")
    myHRP.CFrame = CFrame.new(-83, 19, -130)
    wait(1)  -- Esperar um segundo para garantir que o teleporte inicial foi concluído
    local connection
    connection = game:GetService("RunService").Stepped:Connect(function()
        myHRP.CFrame = targetHRP.CFrame
        -- Verifique se o jogador alvo está sentado
        if targetPlayer.Character and targetPlayer.Character:FindFirstChild("Humanoid") and targetPlayer.Character.Humanoid.Sit then
            -- Jogador alvo está sentado, pare de teletransportar
            myHRP.CFrame = CFrame.new(500000, 0, 0)
            connection:Disconnect()
        end
    end)
end

-- Lista de jogadores para selecionar
local players = game.Players:GetPlayers()
local selectedPlayer

local dropdownOptions = {}
for i, player in ipairs(players) do
    table.insert(dropdownOptions, player.Name)
end

-- Adiciona a nova seção "Trollar jogadores"
mainTab:AddSection({
    Name = "Trollar jogadores"
})

mainTab:AddDropdown({
    Name = "Selecionar Jogador",
    Default = "",
    Options = dropdownOptions,
    Callback = function(value)
        selectedPlayer = game.Players:FindFirstChild(value)
    end
})

mainTab:AddButton({
    Name = "Matar Jogador [precisa de um equipamento]",
    Callback = function()
        if selectedPlayer then
            teleportAndKillPlayer(selectedPlayer)
        else
            print("Nenhum jogador selecionado.")
        end
    end    
})

mainTab:AddButton({
    Name = "Puxar Jogador, nao funciona ainda",
    Callback = function()
        myHRP.CFrame = CFrame.new(0, 0, 0)
    end    
})

-- Atualiza a aba "Teleportes + Visual/Cliente"
visualTab:AddSection({
    Name = "Teleporte para outros lugares (brookhaven only)"
})

visualTab:AddButton({
    Name = "Delegacia",
    Callback = function()
        myHRP.CFrame = CFrame.new(-107, 3, 35)
    end    
})

visualTab:AddButton({
    Name = "Hospital",
    Callback = function()
        myHRP.CFrame = CFrame.new(-333, 4, 36)
    end    
})

visualTab:AddButton({
    Name = "Escola",
    Callback = function()
        myHRP.CFrame = CFrame.new(-324, 4, 203)
    end    
})

visualTab:AddButton({
    Name = "Banco do Brookhaven",
    Callback = function()
        myHRP.CFrame = CFrame.new(12, 3, 268)
    end    
})

visualTab:AddButton({
    Name = "Mercado",
    Callback = function()
        myHRP.CFrame = CFrame.new(-23, 3, -128)
    end    
})

visualTab:AddButton({
    Name = "Shopping",
    Callback = function()
        myHRP.CFrame = CFrame.new(161, 11, -166)
    end    
})

visualTab:AddButton({
    Name = "Aeroporto",
    Callback = function()
        myHRP.CFrame = CFrame.new(356, 14, 107)
    end    
})

visualTab:AddButton({
    Name = "Início/Lobby",
    Callback = function()
        myHRP.CFrame = CFrame.new(0, 0, 0)
    end    
})

-- Adiciona a aba "Alguns itens" com a seção "Itens de lugares"
itemsTab:AddSection({
    Name = "Itens de lugares"
})

itemsTab:AddButton({
    Name = "Bola de Basquete",
    Callback = function()
        myHRP.CFrame = CFrame.new(-137, 2, 98)
    end    
})

itemsTab:AddButton({
    Name = "Macas de Hospital",
    Callback = function()
        myHRP.CFrame = CFrame.new(-343, 2, 34)
    end    
})

itemsTab:AddButton({
    Name = "Aparelho Desconhecido",
    Callback = function()
        myHRP.CFrame = CFrame.new(-272, 2, 60)
    end    
})

itemsTab:AddButton({
    Name = "Aparelho de Zoom",
    Callback = function()
        myHRP.CFrame = CFrame.new(-316, 5, 62)
    end    
})

itemsTab:AddButton({
    Name = "Medicamento",
    Callback = function()
        myHRP.CFrame = CFrame.new(-336, 3, 28)
    end    
})

itemsTab:AddButton({
    Name = "Livro",
    Callback = function()
        myHRP.CFrame = CFrame.new(-333, 4, 170)
    end    
})

itemsTab:AddButton({
    Name = "Guitarra/Violão",
    Callback = function()
        myHRP.CFrame = CFrame.new(-384, 17, 212)
    end    
})

itemsTab:AddButton({
    Name = "Cartão do Cofre",
    Callback = function()
        myHRP.CFrame = CFrame.new(6, 2, 269)
    end    
})

itemsTab:AddButton({
    Name = "Muleta de Dinheiro",
    Callback = function()
        myHRP.CFrame = CFrame.new(-11, 16, 275)
    end    
})

itemsTab:AddButton({
    Name = "Armas/Bombas",
    Callback = function()
        myHRP.CFrame = CFrame.new(-138, -29, 239)
    end    
})

itemsTab:AddButton({
    Name = "Sofá do Club Brooks",
    Callback = function()
        myHRP.CFrame = CFrame.new(-83, 19, -130)
    end    
})

-- Botões para a aba Admin Scripts
adminTab:AddButton({
    Name = "Infinite Yield",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
    end    
})

adminTab:AddButton({
    Name = "Nameless Admin",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/FilteringEnabled/NamelessAdmin/main/Source'))()
    end    
}) 
-- Adiciona a aba "Chat"
local chatTab = Window:MakeTab({
    Name = "Chat",
    Icon = "rbxassetid://8834748103",
    PremiumOnly = false
})

-- Adiciona a seção "Chat bypasser"
chatTab:AddSection({
    Name = "Chat bypasser"
})

-- Função para enviar mensagem com texto específico
local function sendMessage(text)
    game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(text, "All")
end

-- Botão para enviar "Mensagem: YouTube"
chatTab:AddButton({
    Name = "Mensagem: YouTube",
    Callback = function()
        sendMessage("Y͜͡o͜͡u͜͡T͜͡u͜͡b͜͡e͜͡")
    end
})

-- Botão para enviar "Mensagem: Instagram"
chatTab:AddButton({
    Name = "Mensagem: Instagram",
    Callback = function()
        sendMessage("I͜͡n͜͡s͜͡t͜͡a͜͡g͜͡r͜͡a͜͡m͜͡")
    end
})

-- Botão para enviar "Mensagem: Tik tok"
chatTab:AddButton({
    Name = "Mensagem: Tik tok",
    Callback = function()
        sendMessage("T͜͡i͜͡k͜͡ t͜͡o͜͡k͜͡")
    end
})

-- Botão para enviar "Mensagem: Twitter"
chatTab:AddButton({
    Name = "Mensagem: Twitter",
    Callback = function()
        sendMessage("T͜͡w͜͡i͜͡t͜͡t͜͡e͜͡r͜͡")
    end
})

-- Botão para enviar "Mensagem: Kwai"
chatTab:AddButton({
    Name = "Mensagem: Kwai",
    Callback = function()
        sendMessage("k͜͡w͜͡a͜͡i͜͡")
    end
})

-- Botão para enviar "Mensagem: Discord"
chatTab:AddButton({
    Name = "Mensagem: Discord",
    Callback = function()
        sendMessage("D͜͡i͜͡s͜͡c͜͡o͜͡r͜͡d͜͡")
    end
})

-- Botão para enviar "Mensagem: WhatsApp"
chatTab:AddButton({
    Name = "Mensagem: WhatsApp",
    Callback = function()
        sendMessage("w͜͡h͜͡a͜͡t͜͡s͜͡ a͜͡p͜͡p͜͡")
    end
})

-- Botão para enviar "Mensagem: Twitch"
chatTab:AddButton({
    Name = "Mensagem: Twitch",
    Callback = function()
        sendMessage("T͜͡w͜͡i͜͡t͜͡c͜͡h͜͡")
    end
})
-- Inicializando a biblioteca Orion
OrionLib:Init()
